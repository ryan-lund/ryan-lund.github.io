# ryan-lund.github.io

Static resume homepage for Ryan Lund.

## Local preview

```bash
python3 -m http.server 8000 --bind 0.0.0.0
```

Then open `http://localhost:8000/`, or replace `localhost` with the host name or IP address when previewing from another machine.
